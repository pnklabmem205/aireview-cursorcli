# AI 코드 리뷰 결과
# 커밋: 28d5945b53478ff9d4d2e5cd20e3170e1bb88899
# 브랜치: master
# 리뷰 일시: 2026-02-25 02:42:16
# 변경된 파일:
# - reviews/a7a31fe314eceb7dc631937f60f1bf4575014992.md
# - scripts/view-review.sh

---

## 리뷰 결과

**상태**: WARNING  
**요약**: 리뷰 결과 저장 디렉토리 변경 자체는 단순하지만, 기존 설정/데이터와의 호환성과 일관성 측면에서 몇 가지 주의점이 있습니다.

**발견된 이슈**:
- **리뷰 저장 위치 변경에 따른 호환성 이슈 가능성 (`view-review.sh`)**  
  - `REVIEWS_DIR`가 `.git/reviews` → `reviews`로 변경되면서, 기존 `.git/reviews`에 저장된 리뷰들이 더 이상 조회되지 않을 수 있습니다(마이그레이션/호환 로직이 없다면).  
  - 다른 스크립트(예: `switch-review-storage.sh`, `post-commit` hook)가 여전히 `.git/reviews`를 기준으로 동작한다면, 저장 위치와 조회 위치가 불일치하는 상태가 될 수 있습니다.
- **구성 정보 중복/일관성 문제 가능성**  
  - 리뷰 저장 디렉토리 경로가 `view-review.sh` 내부 상수로만 바뀐 상태라면, hook 설정(`.git/hooks/post-commit`)이나 문서 등에서 여전히 `.git/reviews`를 전제로 하고 있을 수 있습니다.  
  - 저장 위치를 하나로 바꾸는 경우, 설정(변수), 문서, 다른 스크립트 간의 경로 정의가 동시에 업데이트되지 않으면 장기적으로 혼란과 버그 가능성이 생깁니다.

**개선 제안**:
- **저장 위치 변경에 대한 마이그레이션/호환 처리**  
  - 기존 `.git/reviews`에 남아 있는 파일이 있는지 검사해서, 있으면 `reviews`로 자동 이동하거나, 최소한 “이전 디렉토리에 리뷰가 남아 있습니다. `switch-review-storage.sh`를 실행하세요” 같은 안내 메시지를 추가하는 것을 고려해주세요.  
  - `view-review.sh`에서 `REVIEWS_DIR="reviews"`를 기본값으로 사용하되, `.git/reviews`에만 파일이 있는 경우 fallback으로 그쪽도 조회해 보는 방식도 생각해볼 수 있습니다.
- **리뷰 저장 위치 설정의 단일 소스화 및 문서 정리**  
  - 리뷰 디렉토리 경로를 한 곳(예: 공통 설정 파일, 환경 변수, 혹은 hook과 스크립트에서 공통으로 `source`하는 설정 스크립트 등)에서 정의하고, `view-review.sh`, `switch-review-storage.sh`, `post-commit` hook이 모두 이를 참조하도록 하면 일관성이 좋아집니다.  
  - 현재 커밋에서 리뷰 저장 위치의 기본값이 `reviews`라면, 관련 문서(README, REVIEW_STORAGE_OPTIONS 등)와 hook 예시 코드도 모두 `reviews` 기준으로 맞춰졌는지 다시 한 번 점검하는 것을 권장드립니다.
